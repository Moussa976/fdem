{% extends 'base.html.twig' %}

{% block title %}Nouveau joueur{% endblock %}

{% block body %}
	<div class="container mt-5 pt-4">
		<h1 class="mb-4 text-center">Ajouter un joueur</h1>
		<div class="row g-2">

			<div class="col-12 my-4">
				<a class="btn btn-sm btn-secondary" href="{{ path('app_joueur_index') }}">Retour à la liste des joueurs</a>
			</div>
		</div>
		<div class="card mt-5">
			<div class="card-body">
				{{ include('joueur/_form.html.twig', {'button_label': 'Ajouter'}) }}
			</div>
		</div>
	</div>
{% endblock %}
