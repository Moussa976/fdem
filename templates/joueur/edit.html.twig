{% extends 'base.html.twig' %}

{% block title %}Modification d'un jouer
{% endblock %}

{% block body %}
	<div class="container mt-5 pt-4">
		<h1 class="mb-4 text-center">Modifier un joueur</h1>
		<div class="row g-2">

			<div class="col-12 my-4">
				<a class="btn btn-sm btn-secondary" href="{{ path('app_joueur_index') }}">Retour à la liste des joueurs</a>
			</div>
			<div class="col-12 col-md-6">
				{{ include('joueur/_delete_form.html.twig') }}
			</div>
		</div>
		<div class="card mt-5">
			<div class="card-body">
				{{ include('joueur/_form.html.twig', {'button_label': 'Modifier'}) }}
			</div>
		</div>
	</div>


{% endblock %}
