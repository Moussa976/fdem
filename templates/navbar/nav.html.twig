<nav class="navbar navbar-expand-lg navbar-dark bg-primary fixed-top">
	<div class="container-fluid">
		<a class="navbar-brand" href="{{path ('app_matche_index')}}">
			<img src="{{ asset('images/ascm-logo.png') }}" alt="Logo" width="40" height="40" class="d-inline-block align-text-top">
			Tableau de bord
		</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarNav">
			<ul class="navbar-nav ms-auto">
				{% if app.user and 'ROLE_ADMIN' in app.user.roles %}
					<li class="nav-item mx-2 my-1">
						<a class="btn btn-sm btn-secondary" href="{{path ('app_matche_new')}}">
							<i class="fas fa-plus-circle"></i>
							Créer Match</a>
					</li>
					<li class="nav-item mx-2 my-1">
						<a class="btn btn-sm btn-success" href="{{path ('app_joueur_index')}}">
							<i class="fas fa-users"></i>
							Joueurs</a>
					</li>
				{% endif %}
				<li class="nav-item mx-2 my-1">
					{% if app.user %}
						<div class="btn-group">
							<button type="button" class="btn btn-sm btn-warning dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">
								<i class="fas fa-user"></i> 
								{% if is_granted('ROLE_EQUIPE') %}
									{{ app.user.equipe.nom}}
								{% elseif is_granted('ROLE_SUPERADMIN') %}
									Super Admin
								{% else %}
									Admin
								{% endif %}
							</button>
							<ul class="dropdown-menu">
								<li>
									<a class="dropdown-item" href="{{ path('app_register') }}">
										<i class="fas fa-gear"></i>
										Gérer les utilisateurs</a>
									<a class="dropdown-item" href="{{ path('app_logout') }}">
										<i class="fas fa-right-from-bracket"></i>
										Se déconnecter</a>
								</li>
							</ul>
						</div>
					{% else %}
						<a class="btn btn-sm btn-warning" href="{{ path('app_login') }}">
							<i class="fas fa-sign-in-alt"></i>
							Se connecter</a>
					{% endif %}

				</li>
			</ul>
		</div>
	</div>
</nav>
