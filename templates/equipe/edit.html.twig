{% extends 'base.html.twig' %}

{% block title %}Modification d'une équipe
{% endblock %}

{% block body %}

		<h1 class="mb-4 text-center">Modifier une équipe</h1>
		<div class="row g-2">

			<div class="col-12 my-4">
				<a class="btn btn-sm btn-secondary" href="{{ path('app_equipe_index') }}">Retour à la liste des équipes</a>
			</div>
			<div class="col-12 col-md-6">
				{{ include('equipe/_delete_form.html.twig') }}
			</div>
		</div>
		<div class="card mt-5">
			<div class="card-body">
				{{ include('equipe/_form.html.twig', {'button_label': 'Modifier'}) }}
			</div>
		</div>



{% endblock %}
