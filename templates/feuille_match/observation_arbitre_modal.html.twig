{{ form_start(form, {'attr': {'id': 'formObservationArbitre'}}) }}
<div class="mb-3 row">
	{{ form_label(form.observationArbitre) }}
	<div class="col-12">
		{{ form_widget(form.observationArbitre, {'attr': {'class': 'form-control'}}) }}
	</div>
</div>
<div class="text-center">
	<button type="submit" class="btn btn-primary mt-3">Enregistrer</button>
</div>
{{ form_end(form) }}
